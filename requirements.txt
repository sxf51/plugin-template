# Authoritative dependency list. The loader parses every non-comment line with
# PEP 508, compares it against installed package metadata, and installs only
# what is missing or version-mismatched before importing your code.
#
# Rejected outright: options (-r, -e, --index-url), local paths, VCS entries
# (git+...), and direct URLs. One plain requirement per line:
#
#   httpx>=0.28,<1
#   PyYAML>=6,<7
#
# This template is deliberately stdlib-only so it loads in any environment.
